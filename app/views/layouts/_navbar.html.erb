<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand active" href="/">InovaTech</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:action] == 'home' %>"><a href="/"><%= t('links.home') %></a></li>
        <li class="<%= 'active' if params[:action] == 'about' %>"><%= link_to t('links.about'), welcome_about_path %></li>
        <li class="<%= 'active' if params[:controller] == 'projects' %>"><%= link_to t('links.project'), projects_path%></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <div class="btn-group navbar-btn">
              <button type="button" class="btn btn-primary"><i class="fa fa-user fa-fw"></i><%= " "+current_user.full_name  %></button>
              <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                <span class="caret"></span>
                <span class="sr-only">Toggle Dropdown</span>
              </button>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to " Editar Perfil", edit_user_registration_path(current_user), class: "fa fa-pencil fa-fw"  %></li>
                <li> <%= link_to " Logout", destroy_user_session_path, method: 'delete', class: "fa fa-trash-o fa-fw" %></li>
              </ul>
            </div>
          <% else %>
          <li class="<%= 'active' if params[:controller] == 'devise/sessions' %>"><%= link_to t('links.login'), "#myModal", data: {toggle: "modal"} %></li>
          <li class="<%= 'active' if params[:controller] == 'devise/registrations' %>"><%= link_to t('links.register'), new_user_registration_path %></li>
          <% end %>
        </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<%= render "layouts/modal_login" %>